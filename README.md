# W32_GUI_input_server

This project is a basic GUI, taking user input, passing it using HTTP GET method and reading back the size of the server answer.
It checks for blank entry but no regex is used.
